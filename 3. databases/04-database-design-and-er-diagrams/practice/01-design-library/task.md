# Практическое задание: Проектирование базы данных

## 📋 Описание

В этом задании вы спроектируете базу данных для **библиотеки книг**.

## 🎯 Цели

1. Определить сущности и атрибуты
2. Нарисовать ER-диаграмму
3. Определить связи между таблицами
4. Применить нормализацию
5. Написать SQL для создания схемы

## 📚 Предметная область: Библиотека

### Требования

1. В библиотеке есть **книги**
2. У каждой книги есть **авторы** (может быть несколько)
3. Книги относятся к **жанрам** (может быть несколько)
4. **Читатели** могут брать книги
5. Нужно хранить историю **выдачи книг** (кто, когда взял, когда вернул)
6. У книг есть **экземпляры** (одна книга — несколько копий)

## 📝 Задания

### Задание 1: Определите сущности

Какие таблицы нужны? Например:
- books (книги)
- authors (авторы)
- ...

### Задание 2: Определите связи

| Сущность 1 | Сущность 2 | Тип связи |
|------------|------------|-----------|
| books | authors | ? |
| books | genres | ? |
| ... | ... | ... |

### Задание 3: ER-диаграмма

Нарисуйте ER-диаграмму (на бумаге или в draw.io).

### Задание 4: Напишите SQL

```sql
-- Создайте базу данных
CREATE DATABASE library;
USE library;

-- Создайте таблицы
-- Ваш код здесь...
```

## 💡 Подсказки

### Структура таблицы books

```sql
CREATE TABLE books (
    book_id INT AUTO_INCREMENT PRIMARY KEY,
    title VARCHAR(255) NOT NULL,
    isbn VARCHAR(20) UNIQUE,
    publication_year YEAR,
    pages INT,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### Связь N:M (книги ↔ авторы)

```sql
CREATE TABLE book_authors (
    book_id INT,
    author_id INT,
    PRIMARY KEY (book_id, author_id),
    FOREIGN KEY (book_id) REFERENCES books(book_id),
    FOREIGN KEY (author_id) REFERENCES authors(author_id)
);
```

### Экземпляры книг

```sql
CREATE TABLE book_copies (
    copy_id INT AUTO_INCREMENT PRIMARY KEY,
    book_id INT NOT NULL,
    copy_number INT,  -- Номер экземпляра
    status ENUM('available', 'borrowed', 'reserved', 'lost') DEFAULT 'available',
    FOREIGN KEY (book_id) REFERENCES books(book_id)
);
```

### История выдачи

```sql
CREATE TABLE loans (
    loan_id INT AUTO_INCREMENT PRIMARY KEY,
    copy_id INT NOT NULL,
    reader_id INT NOT NULL,
    loan_date DATE NOT NULL,
    due_date DATE NOT NULL,
    return_date DATE,
    FOREIGN KEY (copy_id) REFERENCES book_copies(copy_id),
    FOREIGN KEY (reader_id) REFERENCES readers(reader_id)
);
```

## ⏱ Время выполнения

~60-90 минут

## ✅ Критерии выполнения

- [ ] Определены все необходимые сущности
- [ ] Правильно определены типы связей
- [ ] Нарисована ER-диаграмма
- [ ] SQL-код создаёт все таблицы без ошибок
- [ ] Использованы правильные типы данных
- [ ] Добавлены внешние ключи

## 💡 Дополнительные задания

1. Добавьте таблицу **издательств** (publishers)
2. Добавьте систему **штрафов** за просрочку
3. Добавьте **рейтинги** книг от читателей
4. Создайте тестовые данные (INSERT)
