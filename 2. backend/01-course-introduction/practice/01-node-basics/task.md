# Практическое задание: Основы Node.js

## 📋 Описание

В этом задании вы закрепите базовые навыки работы с Node.js.

## 🎯 Цели

1. Убедиться, что Node.js установлен корректно
2. Понять структуру проекта Node.js
3. Попрактиковаться с встроенными модулями

## 📝 Задания

### Задание 1: Проверка установки

1. Откройте терминал
2. Выполните команды:

```bash
node --version
npm --version
```

3. Запишите версии в комментарии вашего кода

### Задание 2: Информация о системе

Создайте файл `system-info.js`, который выводит:

- Вашу операционную систему
- Версию Node.js
- Количество ядер процессора
- Общий объём оперативной памяти
- Текущие дату и время

**Подсказка:** Используйте модули `os` и `process`.

### Задание 3: Калькулятор в командной строке

Создайте файл `calculator.js`, который:

1. Принимает 3 аргумента: число1, оператор, число2
2. Выполняет операцию (+, -, *, /)
3. Выводит результат

**Пример использования:**
```bash
node calculator.js 10 + 5
# Результат: 15

node calculator.js 20 / 4
# Результат: 5
```

**Подсказка:** Используйте `process.argv`.

### Задание 4: Чтение файла

Создайте файл `file-reader.js`, который:

1. Читает содержимое файла `sample.txt` (создайте его)
2. Выводит содержимое в консоль
3. Выводит количество строк и символов

**Подсказка:** Используйте `fs.readFileSync()`.

## ⏱ Время выполнения

~30-45 минут

## ✅ Критерии выполнения

- [ ] Все команды выполняются без ошибок
- [ ] Код содержит комментарии
- [ ] Используются встроенные модули Node.js
- [ ] Калькулятор обрабатывает ошибки (деление на 0, неверный оператор)

## 💡 Дополнительные задания (по желанию)

1. Добавьте в калькулятор поддержку степени (`**`) и остатка от деления (`%`)
2. Сделайте `file-reader.js` асинхронным (используйте `fs.readFile()` с callback)
3. Создайте программу, которая выводит таблицу умножения в файл

## 📚 Полезные ресурсы

- [Node.js Documentation](https://nodejs.org/docs/latest/api/)
- [Process object](https://nodejs.org/docs/latest/api/process.html)
- [OS module](https://nodejs.org/docs/latest/api/os.html)
- [FS module](https://nodejs.org/docs/latest/api/fs.html)
