---
title: "Функция авторизации"
highlight: js
---

# Функция авторизации

Мы научились подсвечивать пункты меню. Но что если пользователь вошел на сайт? Кнопка "Войти" должна превратиться в "Выйти". А еще у него должна появиться ссылка "Мои бронирования" (которая скрыта для гостей).

Для управления этим состоянием мы создадим новую функцию `updateAuthNav()`.

## 🛠 Задание

1. В самом низу файла `main.js` создайте новую пустую функцию `updateAuthNav()`.
2. Вызовите её в самом конце функции `initNavigation()` (после цикла), чтобы она тоже срабатывала при загрузке любой страницы.

```js:start
  // ... конец функции initNavigation ...
  });
}
```

```js:solution
  // ... конец функции initNavigation ...
  });

  updateAuthNav();
}

function updateAuthNav() {
  
}
```
