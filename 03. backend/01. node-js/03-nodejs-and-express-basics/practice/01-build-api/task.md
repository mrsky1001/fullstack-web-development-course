# Практическое задание: Создайте свой API

## 📋 Описание

В этом задании вы создадите собственный REST API для управления списком задач (To-Do List).

## 🎯 Цели

1. Создать Express-сервер
2. Реализовать CRUD-операции
3. Обрабатывать ошибки
4. Использовать правильные HTTP-статусы

## 📦 Подготовка

```bash
# Создайте папку проекта
mkdir todo-api
cd todo-api

# Инициализируйте проект
npm init -y

# Установите Express
npm install express
```

## 📝 Требования

### API Endpoints

Реализуйте следующие маршруты:

| Метод | URL | Описание | Статус |
|-------|-----|----------|--------|
| GET | `/api/todos` | Получить все задачи | 200 |
| GET | `/api/todos/:id` | Получить задачу по ID | 200/404 |
| POST | `/api/todos` | Создать задачу | 201 |
| PUT | `/api/todos/:id` | Обновить задачу | 200/404 |
| DELETE | `/api/todos/:id` | Удалить задачу | 200/404 |

### Структура задачи

```javascript
{
    id: 1,
    title: "Изучить Express",
    description: "Пройти урок 03",
    completed: false,
    createdAt: "2024-01-15T10:00:00.000Z"
}
```

### Требования к ответам

**Успешный ответ:**
```javascript
{
    success: true,
    data: { /* данные */ }
}
```

**Ошибка:**
```javascript
{
    success: false,
    error: "Описание ошибки"
}
```

## 🔧 Подсказки

### Хранение данных

Используйте массив в памяти:

```javascript
let todos = [];
let nextId = 1;
```

### Валидация

- `title` — обязательное поле
- `completed` — по умолчанию `false`

### Поиск по ID

```javascript
const todo = todos.find(t => t.id === parseInt(req.params.id));
```

### Генерация ID

```javascript
const newTodo = {
    id: nextId++,
    title: req.body.title,
    // ...
};
```

## ⏱ Время выполнения

~60-90 минут

## ✅ Критерии выполнения

- [ ] Все 5 endpoints работают корректно
- [ ] Используются правильные HTTP-статусы
- [ ] Реализована базовая валидация
- [ ] Ответы имеют единый формат
- [ ] Код содержит комментарии
- [ ] Обработка 404 для несуществующих маршрутов

## 💡 Дополнительные задания

1. **Фильтрация**: `GET /api/todos?completed=true`
2. **Сортировка**: `GET /api/todos?sort=createdAt`
3. **Поиск**: `GET /api/todos?search=express`
4. **Toggle**: `PATCH /api/todos/:id/toggle` — переключить completed

## 📊 Тестирование

Используйте Postman или Thunder Client для тестирования:

1. **Создайте** 3 задачи (POST)
2. **Получите** список всех (GET /api/todos)
3. **Получите** одну задачу (GET /api/todos/1)
4. **Обновите** задачу (PUT)
5. **Удалите** задачу (DELETE)
6. **Проверьте** 404 для несуществующего ID

## 📚 Пример решения

Структура проекта:

```
todo-api/
├── server.js
├── package.json
└── README.md
```

Шаблон server.js:

```javascript
const express = require('express');
const app = express();
const PORT = 3000;

app.use(express.json());

let todos = [];
let nextId = 1;

// GET /api/todos
app.get('/api/todos', (req, res) => {
    // Ваш код здесь
});

// GET /api/todos/:id
app.get('/api/todos/:id', (req, res) => {
    // Ваш код здесь
});

// POST /api/todos
app.post('/api/todos', (req, res) => {
    // Ваш код здесь
});

// PUT /api/todos/:id
app.put('/api/todos/:id', (req, res) => {
    // Ваш код здесь
});

// DELETE /api/todos/:id
app.delete('/api/todos/:id', (req, res) => {
    // Ваш код здесь
});

// 404 handler
app.use((req, res) => {
    res.status(404).json({ success: false, error: 'Not found' });
});

app.listen(PORT, () => {
    console.log(`Server: http://localhost:${PORT}`);
});
```

Удачи! 🚀
