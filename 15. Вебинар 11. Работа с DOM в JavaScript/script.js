/* ==========================================================
    📘 ТЕМА: РАБОТА С DOM (Document Object Model)
   ==========================================================

DOM (Document Object Model) — это программное представление HTML-документа в виде дерева объектов.
Каждый элемент страницы (тег, текст, атрибут) становится узлом (node), с которым можно взаимодействовать через JavaScript.

------------------------------------------------------------
    🔹 Основные понятия DOM:
------------------------------------------------------------
1. document — это главный объект, представляющий весь HTML-документ.
2. Добавляем только html + css. Элементы (elements) — узлы дерева, например <div>, <p>, <button>.
3. Атрибуты (attributes) — свойства элементов, например id, class, src.
4. Текстовые узлы (text nodes) — содержимое внутри тегов.
5. Манипуляции DOM — изменение структуры, текста, стилей и событий на странице.

=============================================================
    ЧАСТЬ 1. Поиск элементов в DOM
=============================================================
*/


// Получаем элемент по ID
const h1 = document.getElementById("main-title");

// Получаем элементы по классу (возвращает HTMLCollection)
const items = document.getElementsByClassName("list-item");

// Получаем элементы по тегу
const paragraphs = document.getElementsByTagName("p");

// Современные методы — querySelector / querySelectorAll
const button = document.querySelector(".btn"); // первый элемент с классом .btn
const allButtons = document.querySelectorAll(".btn"); // все кнопки с классом .btn

/* ==========================================================
    ЧАСТЬ 2. Добавляем только html + css. Изменение содержимого и атрибутов
============================================================= */

// Изменение текста и HTML
h1.textContent = "Новый заголовок через JS"; // изменяет только текст
h1.innerHTML = "<span style='color: red;'>HTML вставка</span>"; // вставляет HTML-код

// Работа с атрибутами
const image = document.querySelector("img");
image.setAttribute("alt", "Описание картинки"); // добавить или изменить атрибут
image.getAttribute("src"); // получить значение атрибута
image.removeAttribute("title"); // удалить атрибут

// Изменение стилей элемента
h1.style.color = "blue";
h1.style.fontSize = "32px";

/* ==========================================================
    ЧАСТЬ 3. Работа с классами элементов
============================================================= */

const box = document.querySelector(".box");

// Добавление, удаление, переключение классов
box.classList.add("active");
box.classList.remove("hidden");
box.classList.toggle("highlighted"); // включает/выключает класс

// Проверка наличия класса
if (box.classList.contains("active")) {
    console.log("Элемент активен");
}

/* ==========================================================
    ЧАСТЬ 4. Создание и удаление элементов
============================================================= */

// Создание нового элемента
const newParagraph = document.createElement("p");
newParagraph.textContent = "Я добавлен через JavaScript!";
newParagraph.classList.add('color-green')

// Добавление элемента на страницу
const container = document.querySelector(".container");
container.appendChild(newParagraph); // вставляем в конец контейнера

// Добавление в начало
container.prepend(newParagraph);

// Удаление элемента
// newParagraph.remove(); // удаляет элемент из DOM

/* ==========================================================
    ЧАСТЬ 5. Обработка событий
=============================================================

Событие (event) — это любое действие пользователя или браузера:
клик мыши, ввод текста, загрузка страницы и т.д.

Каждый элемент может «слушать» событие и реагировать на него.
============================================================= */

const clickBtn = document.querySelector("#clickBtn");

clickBtn.addEventListener("click", function () {
    console.log("Кнопка нажата!");
    h1.textContent = "Вы кликнули по кнопке!";
    h1.classList.toggle('color-green')
});

/* ==========================================================
    ЧАСТЬ 6. Работа с формами
============================================================= */

const input = document.querySelector("#username");
const form = document.querySelector("#loginForm");

form.addEventListener("submit", (event) => {
    event.preventDefault(); // предотвращаем перезагрузку страницы
    const name = input.value; // получаем введённый текст
    console.log(`Привет, ${name}!`);
});

/* ==========================================================
    ЧАСТЬ 7. Навигация по DOM-дереву
=============================================================

У любого элемента есть связи с другими:

- parentNode / parentElement → родитель
- children → коллекция потомков
- firstChild / lastChild → первый и последний узел
- nextElementSibling / previousElementSibling → соседи
============================================================= */

const list = document.querySelector(".list");
console.log(list.parentElement); // родитель
console.log(list.children); // потомки
console.log(list.firstElementChild); // первый элемент
console.log(list.lastElementChild); // последний элемент

/* ==========================================================
    ЧАСТЬ 8. Мини-практика: динамическое добавление элементов
============================================================= */

const addBtn = document.querySelector("#addItemBtn");
const ul = document.querySelector(".list");

addBtn.addEventListener("click", () => {
    const li = document.createElement("li");
    li.textContent = "Новый элемент списка";
    ul.appendChild(li);
});

/* ==========================================================
    ЧАСТЬ 9. Терминология и ключевые понятия
=============================================================

1. DOM (Document Object Model) — структура документа, представленная в виде дерева объектов.
2. Добавляем только html + css. Node — узел в DOM-дереве (может быть элемент, текст, комментарий).
3. Element — HTML-элемент (например, <div>, <p>, <button>).
4. Attribute — свойство элемента (например, id, class, src).
5. Event — событие, на которое можно подписаться.
6. Event Listener — обработчик события, выполняющий код при его возникновении.
7. innerHTML / textContent — свойства для изменения содержимого элемента.
8. querySelector / querySelectorAll — современные методы поиска элементов.
9. classList — объект для управления CSS-классами.
10. setAttribute / getAttribute — работа с атрибутами элементов.

=============================================================

🔥 Итог:
DOM позволяет полностью управлять структурой и поведением веб-страницы:
— изменять текст и стили,
— добавлять и удалять элементы,
— реагировать на действия пользователя (клики, ввод, отправку форм),
— динамически создавать интерактивные интерфейсы прямо через JavaScript.

=============================================================
*/
