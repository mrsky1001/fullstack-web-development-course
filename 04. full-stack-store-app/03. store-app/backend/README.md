# 🚀 TechParts Store —  Backend

## Готовое решение (Production-)

Это полностью готовый к использованию бэкенд для интернет-магазина компьютерных комплектующих **TechParts Store**.

Включает все необходимые функции: аутентификацию, работу с товарами и корзиной покупок.

---

## 📂 Структура проекта

```
3. -backend/
│
├── 📄 server.js              # Главный файл сервера (Entry Point)
├── 📄 package.json           # Зависимости проекта
├── 📄 .env.example           # Пример файла конфигурации
├── 📄 .gitignore             # Игнорируемые файлы
│
├── 📁 controllers/           # Контроллеры (обработчики запросов)
│   ├── auth.controller.js        # Авторизация (login, register, logout)
│   ├── product.controller.js     # Товары
│   ├── shopping-cart.controller.js # Корзина
│   └── user.controller.js        # Пользователи
│
├── 📁 routers/               # Маршрутизаторы (группировка путей)
│   ├── auth.router.js            # /auth/*
│   ├── product.router.js         # /product/*
│   └── shopping-cart.router.js   # /shopping-cart/*
│
├── 📁 services/              # Сервисы (бизнес-логика и работа с БД)
│   ├── db.service.js             # Подключение к MySQL
│   ├── product.service.js        # Операции с товарами
│   ├── shopping-cart.service.js  # Операции с корзиной
│   └── user.service.js           # Операции с пользователями
│
├── 📁 models/                # Модели данных (классы)
│   ├── product.class.js          # Класс Product
│   ├── user.class.js             # Класс User
│   ├── resObj.class.js           # Стандартный ответ API
│   ├── shoppingCartRow.class.js  # Строка корзины
│   └── shoppingCartProduct.class.js # Товар в корзине
│
├── 📁 middleware/            # Промежуточные обработчики
│   └── auth.middleware.js        # Проверка авторизации
│
└── 📁 lib/                   # Вспомогательные модули
    ├── messages.lib.js           # Сообщения системы
    └── mockProducts.js           # Тестовые данные (без БД)
```

---

## 🏗️ Архитектура

Проект построен по принципу **трёхслойной архитектуры**:

```
┌─────────────────────────────────────────────────────────────┐
│                       Client (Frontend)                      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                     Routers (Маршрутизация)                  │
│  auth.router.js │ product.router.js │ shopping-cart.router  │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                   Controllers (Обработка)                    │
│  auth.controller │ product.controller │ cart.controller      │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                 Services (Бизнес-логика + БД)                │
│  user.service │ product.service │ cart.service │ db.service │
└─────────────────────────────────────────────────────────────┘
                              ↓
┌─────────────────────────────────────────────────────────────┐
│                      MySQL Database                          │
└─────────────────────────────────────────────────────────────┘
```

### Назначение слоёв

| Слой | Назначение | Пример |
|------|------------|--------|
| **Router** | Определяет пути (endpoints) и HTTP методы | `GET /product` |
| **Controller** | Обрабатывает запрос, формирует ответ | Получает данные, возвращает JSON |
| **Service** | Выполняет бизнес-логику, работает с БД | SQL запросы, валидация |
| **Model** | Описывает структуру данных | Класс Product с полями |

---

## 🚀 Быстрый старт

### 1. Установка зависимостей

```bash
npm install
```

### 2. Настройка окружения

```bash
# Скопируйте файл примера
copy .env.example .env

# Отредактируйте .env и укажите свои данные:
# - DB_PASSWORD — пароль MySQL
# - SESSION_SECRET — секретный ключ (сгенерируйте случайную строку)
```

### 3. Создание базы данных

```sql
CREATE DATABASE IF NOT EXISTS storedb;
USE storedb;

-- Таблица пользователей
CREATE TABLE users (
    user_id INT AUTO_INCREMENT PRIMARY KEY,
    user_name VARCHAR(100),
    user_email VARCHAR(255) UNIQUE NOT NULL,
    user_password VARCHAR(255) NOT NULL,
    user_role VARCHAR(50) DEFAULT 'клиент'
);

-- Таблица товаров
CREATE TABLE products (
    product_id INT AUTO_INCREMENT PRIMARY KEY,
    product_name VARCHAR(255) NOT NULL,
    product_price DECIMAL(10, 2) NOT NULL,
    product_category VARCHAR(100),
    product_img VARCHAR(500)
);

-- Таблица корзины
CREATE TABLE shopping_cart (
    item_id INT AUTO_INCREMENT PRIMARY KEY,
    user_id INT NOT NULL,
    product_id INT NOT NULL,
    item_quantity INT DEFAULT 1,
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    FOREIGN KEY (product_id) REFERENCES products(product_id)
);

-- Тестовые данные
INSERT INTO products (product_name, product_price, product_category) VALUES
    ('Intel Core i9-13900K', 55990, 'Процессоры'),
    ('AMD Ryzen 9 7950X', 62990, 'Процессоры'),
    ('NVIDIA GeForce RTX 4090', 159990, 'Видеокарты'),
    ('AMD Radeon RX 7900 XTX', 89990, 'Видеокарты'),
    ('ASUS ROG STRIX Z790-E', 42990, 'Материнские платы');
```

### 4. Запуск сервера

```bash
# Режим разработки (с автоперезагрузкой)
npm run dev

# Или production режим
npm start
```

### 5. Проверка работы

Откройте в браузере: [http://localhost:3000/product](http://localhost:3000/product)

---

## 🔌 API Endpoints

### 🔐 Аутентификация (`/auth`)

| Метод | Путь | Описание | Тело запроса |
|-------|------|----------|--------------|
| POST | `/auth/register` | Регистрация | `{ "name", "email", "password" }` |
| POST | `/auth/login` | Вход | `{ "email", "password" }` |
| POST | `/auth/logout` | Выход | — |
| GET | `/auth/check` | Статус авторизации | — |

### 📦 Товары (`/product`)

| Метод | Путь | Описание |
|-------|------|----------|
| GET | `/product` | Список всех товаров |
| GET | `/product/:id` | Товар по ID |

### 🛒 Корзина (`/shopping-cart`) *— требует авторизации*

| Метод | Путь | Описание | Тело запроса |
|-------|------|----------|--------------|
| GET | `/shopping-cart` | Содержимое корзины | — |
| POST | `/shopping-cart/add` | Добавить товар | `{ "productId", "quantity" }` |
| DELETE | `/shopping-cart/:id` | Удалить товар | — |
| PUT | `/shopping-cart/:id` | Изменить количество | `{ "quantity" }` |

---

## 📋 Переменные окружения

| Переменная | Описание | По умолчанию |
|------------|----------|--------------|
| `PORT` | Порт сервера | `3000` |
| `NODE_ENV` | Режим работы | `development` |
| `DB_HOST` | Хост MySQL | `localhost` |
| `DB_USER` | Пользователь MySQL | `root` |
| `DB_PASSWORD` | Пароль MySQL | — |
| `DB_NAME` | Имя базы данных | `storedb` |
| `DB_CONNECTION_LIMIT` | Лимит соединений | `5` |
| `SESSION_SECRET` | Секретный ключ сессий | — |
| `FRONTEND_URL` | URL фронтенда (для CORS) | `http://localhost:8000` |

---

## 📦 Зависимости

### Production

| Пакет | Назначение |
|-------|------------|
| `express` | Веб-фреймворк |
| `mysql2` | Драйвер MySQL |
| `dotenv` | Переменные окружения |
| `bcryptjs` | Хеширование паролей |
| `passport` | Аутентификация |
| `passport-local` | Стратегия логин/пароль |
| `express-session` | Сессии |
| `cors` | CORS заголовки |

### Development

| Пакет | Назначение |
|-------|------------|
| `nodemon` | Автоперезагрузка сервера |

---

## 🧪 Тестирование API

### С помощью Postman

1. Импортируйте коллекцию (если есть) или создайте запросы вручную
2. Для защищённых маршрутов сначала выполните `/auth/login`
3. Убедитесь, что cookies сохраняются между запросами

### С помощью curl

```bash
# Регистрация
curl -X POST http://localhost:3000/auth/register \
  -H "Content-Type: application/json" \
  -d '{"name":"Тест","email":"test@mail.com","password":"123456"}'

# Получение товаров
curl http://localhost:3000/product
```

---

## ⚠️ Важные замечания

### Безопасность

- 🔒 В production **обязательно** замените `SESSION_SECRET` на длинную случайную строку
- 🔒 Включите `httpOnly`, `secure` и `sameSite` для cookies (автоматически в NODE_ENV=production)
- 🔒 Используйте HTTPS в production

### Mock-данные

Если MySQL недоступен, сервер **продолжит работать** с тестовыми данными из `lib/mockProducts.js`. Однако:
- ❌ Регистрация/вход не будут сохраняться
- ❌ Корзина не будет работать
- ✅ Товары будут отображаться из mock-данных

---

## 🛠️ Скрипты

```bash
npm start         # Запуск в production режиме
npm run dev       # Запуск с nodemon (автоперезагрузка)
```

---

## 📄 Лицензия

MIT License — IEEU International East-European University

---

## 👥 Авторы

**Курс Full-Stack Web Development**  
Международный Восточно-Европейский Университет (IEEU)

---

> 💡 **Совет:** Для изучения кода по шагам перейдите в папку `../1. step-by-step/` — там каждый урок объясняет одну концепцию.
