# =========================================
# ФАЙЛ КОНФИГУРАЦИИ ОКРУЖЕНИЯ (.env) (УЧЕБНЫЙ ПРИМЕР!)
# =========================================
# Скопируйте этот файл как .env и заполните своими значениями:
#   cp .env.example .env
#
# ВАЖНО: Файл .env НЕ должен попадать в Git (добавлен в .gitignore)!
# =========================================

# --- Настройки сервера ---
PORT=3000
NODE_ENV=development

# --- Настройки базы данных MySQL ---
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=your_password_here
DB_NAME=storedb
DB_CONNECTION_LIMIT=5

# --- Настройки сессии ---
# Секретный ключ для подписи cookies (должен быть длинным и случайным!)
# Генератор: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
SESSION_SECRET=your-super-secret-key-change-in-production

# --- Настройки CORS ---
# URL фронтенда (для production укажите реальный домен)
FRONTEND_URL=http://localhost:8000
