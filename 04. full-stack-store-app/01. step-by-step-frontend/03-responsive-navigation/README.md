# Урок 03: Адаптивная верстка и навигация

## 🎯 Цели урока

После завершения этого урока вы будете:
- Понимать принцип Mobile First
- Уметь использовать media queries
- Создавать фиксированную навигацию
- Управлять DOM с помощью JavaScript

## 📝 Что добавлено в этом уроке

### Структура файлов

```
03-responsive-navigation/
├── index.html
├── css/
│   └── style.css
├── js/
│   └── menu.js      # НОВОЕ: JavaScript для меню
└── README.md
```

### Фиксированная навигация

```css
.navbar {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 1000;
    
    /* Эффект "матового стекла" */
    background: rgba(11, 12, 21, 0.8);
    backdrop-filter: blur(12px);
}
```

### Media Queries

```css
/* Стили для экранов до 768px */
@media (max-width: 768px) {
    .mobile-menu-btn {
        display: block;
    }
    
    .nav-links {
        position: fixed;
        right: -100%;
        /* ... */
    }
    
    .nav-links.active {
        right: 0;
    }
}
```

### JavaScript для мобильного меню

```javascript
// Находим элементы
const mobileMenuBtn = document.querySelector('.mobile-menu-btn');
const navLinks = document.querySelector('.nav-links');

// При клике переключаем класс
mobileMenuBtn.addEventListener('click', () => {
    navLinks.classList.toggle('active');
});
```

## 💡 Ключевые концепции

### Mobile First

Это подход, при котором сначала пишутся стили для мобильных устройств, а затем для больших экранов:

```css
/* Базовые стили (мобильные) */
.container {
    padding: 1rem;
}

/* Стили для больших экранов */
@media (min-width: 768px) {
    .container {
        padding: 2rem;
    }
}
```

### CSS-анимации

```css
.feature-card {
    transition: transform 0.2s;
}

.feature-card:hover {
    transform: translateY(-5px);
}
```

### Работа с DOM

| Метод | Описание |
|-------|----------|
| `querySelector()` | Находит первый элемент по селектору |
| `querySelectorAll()` | Находит все элементы |
| `classList.add()` | Добавляет класс |
| `classList.remove()` | Удаляет класс |
| `classList.toggle()` | Переключает класс |
| `addEventListener()` | Добавляет обработчик события |


## 🔍 Проверьте работу

1. Откройте `index.html` в браузере
2. Сожмите окно браузера до ширины < 768px
3. Появится кнопка-гамбургер
4. Нажмите на неё — выедет меню
5. Нажмите ещё раз — меню скроется

## 📱 Адаптивный дизайн

| Ширина экрана | Устройство | Изменения |
|---------------|------------|-----------|
| > 768px | Десктоп | Полное меню |
| ≤ 768px | Планшет/телефон | Гамбургер-меню |

## 📚 Дополнительные материалы

- [MDN: Media Queries](https://developer.mozilla.org/ru/docs/Web/CSS/Media_Queries)
- [MDN: Position](https://developer.mozilla.org/ru/docs/Web/CSS/position)
- [JavaScript DOM](https://developer.mozilla.org/ru/docs/Web/API/Document_Object_Model)
