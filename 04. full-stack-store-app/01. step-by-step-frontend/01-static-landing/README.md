# Урок 01: Статическая стартовая страница

## 🎯 Цели урока

После завершения этого урока вы будете:
- Понимать базовую структуру HTML-документа
- Знать назначение основных тегов (`<head>`, `<body>`, `<header>`, `<main>`, `<footer>`)
- Уметь создавать семантическую разметку страницы

## 📝 Что добавлено в этом уроке

### Структура HTML-документа

```html
<!DOCTYPE html>     <!-- Объявление типа документа -->
<html lang="ru">   <!-- Корневой элемент с указанием языка -->
  <head>...</head>  <!-- Метаинформация (не отображается) -->
  <body>...</body>  <!-- Видимое содержимое страницы -->
</html>
```

### Мета-теги в `<head>`

| Тег | Назначение |
|-----|------------|
| `<meta charset="UTF-8">` | Кодировка символов (поддержка кириллицы) |
| `<meta name="viewport">` | Адаптивность для мобильных устройств |
| `<title>` | Заголовок вкладки браузера |

### Семантические теги

- **`<header>`** — шапка сайта (логотип, навигация)
- **`<nav>`** — блок навигации
- **`<main>`** — основное содержимое страницы
- **`<section>`** — логический раздел страницы
- **`<article>`** — самодостаточный блок контента
- **`<footer>`** — подвал сайта

## 💡 Ключевые концепции

### Почему семантика важна?

1. **Доступность (Accessibility)** — экранные читалки понимают структуру
2. **SEO** — поисковики лучше индексируют контент
3. **Читаемость кода** — другие разработчики быстрее разберутся

### Правило "один H1 на страницу"

Тег `<h1>` должен быть только один — это главный заголовок страницы.
Остальные заголовки (`<h2>`, `<h3>` и т.д.) создают иерархию.

## 🔍 Откройте index.html в браузере

Вы увидите простую страницу с текстом. Стилей пока нет — весь контент отображается браузером по умолчанию. В следующем уроке мы добавим CSS!

## 📚 Дополнительные материалы

- [MDN: Введение в HTML](https://developer.mozilla.org/ru/docs/Learn/HTML/Introduction_to_HTML)
- [Семантический HTML](https://developer.mozilla.org/ru/docs/Glossary/Semantics)
