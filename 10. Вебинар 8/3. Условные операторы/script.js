/********************************************
 * УСЛОВНЫЕ ОПЕРАТОРЫ В JAVASCRIPT
 ********************************************

  Условные операторы — это конструкции, которые позволяют
 программе выполнять разные действия в зависимости от условий.

 Если условие истинно (true) → выполняется один блок кода.
 Если ложно (false) → другой.

 JavaScript поддерживает несколько способов записи условий:
 - if / else if / else
 - тернарный оператор (?:)
 - switch / case
 ********************************************/


/*---------------------------------------------------
  1. Оператор if
---------------------------------------------------

 Синтаксис:
if (условие) {
    // код, если условие истинно
}

Если выражение в скобках возвращает true, выполняется тело if.
*/


const user = {
    name: "Ivan",
    age:19,
    isSmallAccess: false
    isAccess: false
}

if (user.age >= 18) {
    console.log(" Доступ разрешён — вам 18 или больше.");
    user.isAccess = true
} else if(user.age === 16) {
    user.isSmallAccess = true
} else {

}

if(user.isAccess === true){
    //...
}

/*
Если age меньше 18 — код внутри if не выполнится.
*/


/*---------------------------------------------------
  2. Оператор if...else
---------------------------------------------------

 else (иначе) выполняется, когда условие ложно.
*/

let temperature = 10;

if (temperature >= 20) {
    console.log("На улице тепло ");
} else {
    console.log("На улице прохладно ");
}


/*---------------------------------------------------
  3. Оператор if...else if...else
---------------------------------------------------

 else if позволяет проверять несколько условий подряд.
*/

let score = 75;

if (score >= 90) {
    console.log("Оценка: A (отлично)");
} else if (score >= 70) {
    console.log("Оценка: B (хорошо)");
} else if (score >= 50) {
    console.log("Оценка: C (удовлетворительно)");
} else {
    console.log("Оценка: F (неудовлетворительно)");
}


/*---------------------------------------------------
  4. Вложенные условия (nested if)
---------------------------------------------------

 Одно условие может содержать другое.
Иногда это нужно, если решения зависят от нескольких факторов.
*/

let isStudent = true;
let hasTicket = true;

if (isStudent) {
    if (hasTicket) {
        console.log("Проход разрешён ");
    } else {
        console.log("Нужно купить билет ");
    }
} else {
    console.log("Доступ только для студентов ");
}


/*---------------------------------------------------
  5. Тернарный оператор (?:)
---------------------------------------------------

 Это короткая форма записи if...else.
Синтаксис:
условие ? значение_если_true : значение_если_false
*/

let userAge = 20;
let message = (userAge >= 18) ? "Можно войти " : "Вход запрещён ";

console.log(message);

/*
 Работает как сокращённый if...else.
Удобен для простых проверок.
*/


/*---------------------------------------------------
  6. Оператор switch
---------------------------------------------------

 switch используется, когда нужно проверить одно значение
по множеству возможных вариантов.

Синтаксис:
switch (выражение) {
  case значение1:
    // действия
    break;
  case значение2:
    // действия
    break;
  default:
    // если ни один case не подошёл
}
*/

let day = 3;

switch (day) {
    case 1:
        console.log("Понедельник");
        break;
    case 2:
        console.log("Вторник");
        break;
    case 3:
        console.log("Среда");
        break;
    case 4:
        console.log("Четверг");
        break;
    case 5:
        console.log("Пятница");
        break;
    default:
        console.log("Выходной день 🎉");
}


/*
 break останавливает выполнение после совпадения.
Если его не написать, выполнение продолжится в следующем case.
*/


/*---------------------------------------------------
  7. Пример без break — эффект «проваливания» (fall-through)
---------------------------------------------------*/

let fruit = "яблоко";

switch (fruit) {
    case "яблоко":
    case "груша":
    case "слива":
        console.log("Это фрукт ");
        break;
    case "морковь":
    case "капуста":
        console.log("Это овощ ");
        break;
    default:
        console.log("Неизвестный продукт");
}

/*
 Несколько case могут указывать на одно действие.
*/


/*---------------------------------------------------
  8. Истинность и ложность (truthy / falsy)
---------------------------------------------------

 В JavaScript любое выражение можно привести к логическому значению (true или false).

Falsy (ложные) значения:
- false
- 0
- "" (пустая строка)
- null
- undefined
- NaN

Все остальные значения считаются truthy (истинными).
*/

let testValue = "";

if (testValue) {
    console.log("Истинное значение ");
} else {
    console.log("Ложное значение ");
}

// Выведет "Ложное значение ", потому что "" — falsy


/*---------------------------------------------------
  9. Сравнение значений (напоминание)
---------------------------------------------------

 В условиях часто используют операторы сравнения:

==   сравнение по значению (может преобразовывать типы)
===  строгое сравнение (тип и значение)
!=   не равно
!==  строго не равно
>    больше
<    меньше
>=   больше или равно
<=   меньше или равно
*/

let a = 5;
let b = "5";

if (a == b) console.log("== возвращает true (только значение)");
if (a === b) console.log("=== возвращает true (тип и значение совпадают)");
else console.log("=== возвращает false (тип другой)");

/*
 Используй === (строгое сравнение) — это хорошая практика.
*/


/*---------------------------------------------------
  10. Подведение итогов
---------------------------------------------------

 Основные условные конструкции в JS:

 if — проверяет одно условие
 if...else — добавляет альтернативу
 else if — цепочка проверок
 switch — множество ветвей для одного значения
 тернарный оператор (?:) — короткая форма if...else

 Условия — это логическая основа программирования.
Они позволяют "реагировать" на разные ситуации
и управлять потоком выполнения программы.

---------------------------------------------------*/
