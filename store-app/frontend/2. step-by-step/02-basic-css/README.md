# Урок 02: Базовые стили CSS

## 🎯 Цели урока

После завершения этого урока вы будете:
- Понимать, как подключить CSS к HTML
- Знать назначение CSS Reset
- Уметь использовать CSS-переменные (Custom Properties)
- Создавать простые компоненты: кнопки, карточки, шапку

## 📝 Что добавлено в этом уроке

### Структура файлов

```
02-basic-css/
├── index.html      # HTML с классами для стилизации
├── css/
│   └── style.css   # Таблица стилей
└── README.md
```

### Подключение CSS к HTML

```html
<link rel="stylesheet" href="./css/style.css">
```

### CSS-переменные

```css
:root {
    --color-primary: #6e56cf;
    --spacing-sm: 1rem;
}

.btn {
    background: var(--color-primary);
    padding: var(--spacing-sm);
}
```

**Преимущества переменных:**
- Единое место для изменения значений
- Легко создать систему дизайна
- Поддержка тем (светлая/темная)

### CSS Reset

```css
*, *::before, *::after {
    box-sizing: border-box;
    margin: 0;
    padding: 0;
}
```

Зачем нужен Reset:
- Браузеры имеют разные встроенные стили
- Reset обеспечивает единообразие на всех платформах

### Flexbox для шапки

```css
.header .container {
    display: flex;
    justify-content: space-between;
    align-items: center;
}
```

### CSS Grid для карточек

```css
.features-grid {
    display: grid;
    grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
    gap: 2rem;
}
```

## 💡 Ключевые концепции

### Box Model (Блочная модель)

```
┌─────────────────────────────────┐
│           margin                │
│   ┌───────────────────────┐     │
│   │       border          │     │
│   │   ┌───────────────┐   │     │
│   │   │    padding    │   │     │
│   │   │   ┌───────┐   │   │     │
│   │   │   │content│   │   │     │
│   │   │   └───────┘   │   │     │
│   │   └───────────────┘   │     │
│   └───────────────────────┘     │
└─────────────────────────────────┘
```

`box-sizing: border-box` — padding и border включаются в width.

### Единицы измерения

| Единица | Описание |
|---------|----------|
| `px` | Пиксели (абсолютная) |
| `rem` | Относительно размера шрифта html |
| `%` | Процент от родителя |
| `fr` | Доля доступного пространства (Grid) |

## ✅ Что должен понять студент

- [ ] CSS-файл подключается через `<link>` в `<head>`
- [ ] Селекторы: элемент (`p`), класс (`.btn`), ID (`#header`)
- [ ] CSS-переменные объявляются в `:root`
- [ ] Flexbox — для одномерных раскладок
- [ ] Grid — для двумерных раскладок (сетки)

## 🔍 Откройте index.html в браузере

Теперь страница имеет:
- Тёмную тему
- Стильную шапку
- Красивую кнопку
- Карточки преимуществ

## 📚 Дополнительные материалы

- [MDN: CSS первые шаги](https://developer.mozilla.org/ru/docs/Learn/CSS/First_steps)
- [CSS Variables](https://developer.mozilla.org/ru/docs/Web/CSS/Using_CSS_custom_properties)
- [Flexbox Guide](https://css-tricks.com/snippets/css/a-guide-to-flexbox/)
- [Grid Guide](https://css-tricks.com/snippets/css/complete-guide-grid/)
