# 🎓 TechParts: Учебный проект Fullstack магазина

Добро пожаловать в учебный проект по веб-разработке!
Это **Полноценный интернет-магазин**, созданный для обучения студентов основам взаимодействия Frontend и Backend, работе с базами данных и аутентификацией.

---

## 📚 О проекте и целях обучения

Этот проект демонстрирует классическую архитектуру веб-приложения:
- **Frontend (Клиент)**: То, что видит пользователь. Написан на чистом HTML, CSS, JS.
- **Backend (Сервер)**: Обрабатывает данные, безопасность и логику. Написан на Node.js.
- **Database (База данных)**: Хранит информацию о товарах и пользователях. Используется MySQL.

### 🎯 Чему вы научитесь:
1. Понимать, как браузер общается с сервером (HTTP, REST API).
2. Писать серверную логику на Node.js (Express).
3. Работать с реляционными базами данных (MySQL, SQL).
4. Реализовывать регистрацию и вход пользователей.
5. Создавать динамический интерфейс на Vanilla JS (без сложных фреймворков).

---

## 🛠 Технологии

### Frontend (Клиентская часть)
*Папка: `frontend/`*
- **HTML5**: Структура страниц.
- **CSS3**: Стилизация (Flexbox, Grid, адаптивность).
- **JavaScript (ES6+)**: Логика интерфейса, отправка запросов на сервер (fetch).

### Backend (Серверная часть)
*Папка: `backend/`*
- **Node.js**: Платформа для выполнения JS на сервере.
- **Express.js**: Фреймворк для создания веб-сервера.
- **Passport.js**: Библиотека для аутентификации (входа/регистрации).
- **MySQL2**: Библиотека для подключения к базе данных.

### Database (Хранение данных)
- **MySQL**: Реляционная база данных (таблицы, связи).

---

## 📂 Структура проекта

### Backend (`/backend`)
Здесь лежит логика сервера. Мы используем паттерн **MVC (Model-View-Controller)**, но без View (так как это API):

- **`server.js`**: 🚪 Входная точка. Запускает сервер и подключает все настройки.
- **`controllers/`**: 🎮 **Контроллеры**. Принимают запросы, решают что делать и отправляют ответ. (Например: `auth.controller.js` обрабатывает вход).
- **`services/`**: ⚙️ **Сервисы**. Вся "грязная" работа: запросы в базу данных, сложные вычисления.
- **`routers/`**: 🚏 **Маршрутизаторы**. Направляют запрос на нужный контроллер (например, `/auth/login` -> `auth.controller.login`).
- **`models/`**: 📦 **Модели**. Классы (шаблоны) данных. Помогают превратить "сырой" ответ базы данных в удобный объект JS.

### Frontend (`/frontend`)
Здесь лежит все, что загружается в браузер пользователя:

- **`index.html`**: Главная страница.
- **`css/`**: Файлы стилей.
- **`js/`**: Скрипты.
  - `api/`: Функции для связи с сервером.
  - `components/`: Управление элементами страницы (корзина, карточки товаров).
- **`pages/`**: Дополнительные страницы (вход, регистрация).

---

## 📖 Гид по страницам

### 1. Главная страница (`index.html`)
Витрина магазина. Здесь загружается список товаров с сервера (`GET /product/all`).
Пользователь может добавить товар в корзину. Если он не авторизован - сайт предложит войти.

### 2. Страница входа (`login.html`)
Форма с email и паролем. Отправляет `POST /auth/login`. При успехе сервер создает **сессию**.

### 3. Регистрация (`registration.html`)
Создание нового пользователя. Пароль хешируется (шифруется) перед сохранением в базу.

### 4. Корзина
Это не отдельная страница, а модальное окно (или выезжающая панель).
Данные корзины хранятся в базе данных. Когда вы добавляете товар, запрос летит на сервер (`POST /shopping-cart/add`).

---

## 🔄 Взаимодействие Frontend ↔ Backend

Весь обмен данными происходит через **REST API**.
Frontend "просит" данные, Backend "отвечает".

**Пример (загрузка товаров):**
1. **Frontend**: "Эй, сервер, дай мне список товаров!" (`GET /product/all`)
2. **Backend**:
   - Принимает запрос.
   - Идет в Базу Данных (`SELECT * FROM products`).
   - Получает данные.
   - Отправляет их обратно в формате JSON.
3. **Frontend**: Получает JSON, перебирает его циклом и рисует красивые карточки товаров.

---

## 🔐 Аутентификация (Как работает вход)

Мы используем **Сессии (Sessions)**.

1. Пользователь вводит логин/пароль.
2. Сервер проверяет их. Если ок -> создает сессию (файл на сервере или запись в памяти) и выдает пользователю уникальный **Cookie** (ID сессии).
3. Браузер автоматически сохраняет этот Cookie.
4. При каждом следующем запросе браузер сам прикрепляет этот Cookie.
5. Сервер видит Cookie -> находит сессию -> понимает, кто делает запрос.

---

## 🗄️ База данных (MySQL)

Проект использует 3 основные таблицы:

1. **`users`** (Пользователи):
   - `user_id` (PK)
   - `user_email`
   - `user_password` (Хэш!)
   - `user_role`

2. **`products`** (Товары):
   - `product_id` (PK)
   - `product_name`
   - `product_price`
   - `product_img`

3. **`shopping_cart`** (Корзина):
   - `item_id` (PK)
   - `user_id` (FK -> users)
   - `product_id` (FK -> products)
   - `item_quantity` (Количество)

### 📁 Файл схемы базы данных

Для создания базы данных используйте файл `database/schema.sql`:

```bash
# Из папки проекта
mysql -u root -p < database/schema.sql
```

> 💡 **Без MySQL?** Проект автоматически переключится на Mock-данные!

---

## 🚀 Как запустить проект

### Шаг 0: Подготовка (Предварительные требования)
У вас должны быть установлены:
- **Node.js**: Скачать с [nodejs.org](https://nodejs.org/) (LTS версия).
- **MySQL**: (Опционально, если хотите локальную БД, но проект поддерживает Mock-данные).

### Шаг 1: Запуск Backend
1. Откройте терминал.
2. Перейдите в папку сервера:
   ```bash
   cd backend
   ```
3. Установите зависимости (библиотеки):
   ```bash
   npm install
   ```
4. **(Опционально)** Настройте переменные окружения:
   ```bash
   # Скопируйте шаблон .env
   cp .env.example .env

   # Отредактируйте .env, указав свои настройки БД
   ```
5. Запустите сервер:
   ```bash
   npm start
   ```
   *Вы увидите: "🚀 Сервер запущен на http://localhost:3000"*

### Шаг 2: Запуск Frontend
1. Откройте **новый** терминал.
2. Перейдите в папку фронтенда:
   ```bash
   cd frontend
   ```
3. Запустите локальный сервер (нужно для работы модулей и CORS):
   ** С помощью Node.js:**
   ```bash
   npx http-server -p 8000
   ```
   или используйте **Live Server** в VS Code.

4. Откройте в браузере: `http://localhost:8000`

---

## 📖 Глоссарий терминов

Мы вынесли список всех используемых технологий и терминов с простыми объяснениями в отдельный файл.  
👉 **[Открыть ГЛОССАРИЙ (GLOSSARY.md)](./GLOSSARY.md)**

Там вы найдете ответы на вопросы:
- Что такое Frontend и Backend?
- Зачем нужен API?
- Как работают Сессии и Куки?
- Что такое SQL и MySQL?

---

**Удачного кодинга!** 🚀
Если что-то не работает — читайте ошибки в консоли (F12 в браузере или терминал сервера). Это лучший способ научиться.
