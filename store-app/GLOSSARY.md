# 📖 Глоссарий терминов веб-разработки

Здесь собраны основные технологии и термины, используемые в проекте **TechParts**, с простыми объяснениями для начинающих.

---

## 🌐 Общие понятия

### Frontend (Фронтенд)
**Клиентская часть.** То, что видит пользователь в браузере. Кнопки, формы, анимации, картинки.  
*В этом проекте:* HTML, CSS, JavaScript в папке `frontend/`.

### Backend (Бэкенд)
**Серверная часть.** "Мозги" сайта, которые работают на удаленном компьютере (сервере). Бэкенд обрабатывает заказы, проверяет пароли и работает с базой данных.  
*В этом проекте:* Node.js сервер в папке `backend/`.

### Fullstack (Фулстек)
Разработка и фронтенда, и бэкенда одновременно. Умение создать сайт целиком, от кнопки "Купить" до сохранения заказа в базу данных.

### Client-Server Architecture (Клиент-Серверная архитектура)
Способ устройства веба, где есть **Клиент** (браузер), который *просит* данные, и **Сервер**, который их *отдает*.

### Localhost
**"Этот компьютер".** Специальное имя для вашего собственного компьютера в сети. Когда вы запускаете сервер локально, вы обращаетесь к нему как `localhost` (или `127.0.0.1`).

---

## 🖥️ Frontend Технологии

### HTML (HyperText Markup Language)
**Скелет сайта.** Язык разметки, который говорит браузеру: "Здесь заголовок, здесь картинка, а здесь кнопка".

### CSS (Cascading Style Sheets)
**Одежда сайта.** Язык стилей, который задает цвета, шрифты, отступы и расположение элементов. Без CSS любой сайт выглядит как скучный текстовый документ.

### JavaScript (JS)
**Мышцы сайта.** Язык программирования, который делает страницу живой. Он реагирует на клики, отправляет данные на сервер и обновляет страницу без перезагрузки.

### DOM (Document Object Model)
**Дерево страницы.** Представление HTML-страницы в виде объектов, которыми может управлять JavaScript.
*Пример:* `document.getElementById('btn')` — мы ищем кнопку в DOM-дереве.

### Fetch API
Встроенная в браузер функция (`fetch`), позволяющая JavaScript отправлять запросы на сервер и получать ответы. Это основа современного веба (AJAX).

### LocalStorage
**Память браузера.** Маленькая "база данных" прямо в браузере пользователя. Позволяет сохранять настройки (например, темную тему) даже после закрытия вкладки.

---

## ⚙️ Backend Технологии

### Node.js
Среда, позволяющая запускать JavaScript **вне браузера** (на сервере). Благодаря ей мы можем писать бэкенд на том же языке, что и фронтенд.

### Express.js
Самый популярный фреймворк для Node.js. Упрощает создание веб-сервера. Вместо написания сотни строк кода для обработки одного запроса, мы пишем 2-3 строки.

### NPM (Node Package Manager)
Магазин бесплатных библиотек (кода, написанного другими). Мы используем его для установки `express`, `mysql2`, `passport` и других модулей.
*Команда:* `npm install`.

### API (Application Programming Interface)
Набор "ручек" (ссылок), за которые может дергать Фронтенд, чтобы общаться с Бэкендом.

### REST API
Правила хорошего тона для создания API. Использует стандартные методы HTTP:
- **GET**: Дай данные (получить список товаров).
- **POST**: Создай данные (добавить товар в корзину).
- **PUT**: Обнови данные (изменить количество).
- **DELETE**: Удали данные (удалить товар).

### JSON (JavaScript Object Notation)
**Язык общения.** Формат передачи данных между клиентом и сервером. Выглядит как текст, но легко превращается в JS-объект.
*Пример:* `{"id": 1, "name": "Процессор"}`.

### Middleware (Промежуточное ПО)
Функция-посредник, которая встает МЕЖДУ получением запроса и его обработкой.
*Пример:* Охранник на входе в клуб. Он проверяет паспорт (авторизацию) *до* того, как пустить вас в бар (к контроллеру).

---

## 🔐 Безопасность и Авторизация

### Authentication (Аутентификация)
Процесс проверки: **"Кто ты?"**. Ввод логина и пароля.

### Authorization (Авторизация)
Процесс проверки: **"А можно ли тебе?"**. Проверка прав доступа (например, может ли пользователь зайти в админку).

### Passport.js
Библиотека для Node.js, которая берет на себя сложную работу по входу пользователей. Поддерживает "Стратегии" (Логин/Пароль, Google, Facebook).

### Session (Сессия)
Способ сервера "запомнить" пользователя. Сервер создает файл (или запись в памяти) с данными пользователя и дает ему номерок.

### Cookie (Куки)
Тот самый "номерок" от сессии. Маленькая строчка текста, которую браузер хранит у себя и **автоматически** показывает серверу при каждом запросе.

### BCrypt (Hashing)
Способ превратить пароль в набор непонятных символов (хэш). Мы храним в базе только хэш. Даже если базу украдут, пароли пользователей останутся в тайне.

---

## 🗄️ База данных

### SQL (Structured Query Language)
Язык для общения с реляционными базами данных.
*Пример:* `SELECT * FROM users` (Дай мне всех пользователей).

### MySQL
Конкретная программа (СУБД), которая хранит данные. Самая популярная в мире веб-разработки.

### Relational Database (Реляционная БД)
База данных, где информация хранится в **Таблицах**, которые связаны друг с другом.
*Пример:* Таблица `Orders` связана с таблицей `Users`, чтобы знать, чей это заказ.

### Primary Key (Первичный ключ)
Уникальный ID записи. У каждого пользователя и товара есть свой уникальный номер (id), который не повторяется.

---

**TechParts Education Project** 🎓
