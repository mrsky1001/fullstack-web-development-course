# Урок 1: Базовый HTTP сервер на Node.js

## 🎯 Цель урока

Познакомиться с Node.js и создать простой HTTP сервер без использования дополнительных библиотек.

## 📚 Изучаемые концепции

1. **Что такое Node.js** — среда выполнения JavaScript вне браузера
2. **Встроенный модуль `http`** — создание веб-серверов
3. **Функция `require()`** — подключение модулей
4. **Объекты `request` и `response`** — обработка HTTP запросов
5. **HTTP статус коды** — 200 (OK), 404 (Not Found)
6. **Content-Type заголовок** — указание типа контента (HTML, JSON)

## 📁 Структура проекта

```
01-basic-node-server/
├── src/
│   └── server.js      # Главный файл сервера
├── package.json       # Конфигурация проекта
└── README.md          # Этот файл
```

## 🚀 Запуск сервера

```bash
# 1. Перейдите в папку урока
cd lessons/01-basic-node-server

# 2. Запустите сервер
npm start
```

## ✅ Ожидаемый результат

После запуска сервер будет доступен по адресу:

| Маршрут | Описание |
|---------|----------|
| `http://localhost:3000/` | Главная страница (HTML) |
| `http://localhost:3000/about` | Страница "О проекте" (HTML) |
| `http://localhost:3000/api/status` | API endpoint (JSON) |

## 🔑 Ключевые моменты

### Создание сервера

```javascript
const http = require('http');

const server = http.createServer((req, res) => {
    // req - информация о запросе
    // res - объект для отправки ответа
});
```

### Отправка ответа

```javascript
// Устанавливаем статус и заголовки
res.writeHead(200, { 'Content-Type': 'text/html' });

// Отправляем тело ответа
res.end('<h1>Привет!</h1>');
```

### Запуск сервера

```javascript
server.listen(3000, 'localhost', () => {
    console.log('Сервер запущен!');
});
```

## 📝 Задания для практики

1. Добавьте новый маршрут `/contact` с вашими контактными данными
2. Измените порт сервера на 8080
3. Добавьте в ответ `/api/status` информацию о текущей памяти

## ➡️ Что дальше?

В следующем уроке мы установим **Express.js** — фреймворк, который значительно упрощает создание веб-серверов и управление маршрутами.
