# Урок 05: Блочная модель и позиционирование CSS

## 🎯 Цели урока

После завершения этого урока вы сможете:

- Понимать **блочную модель** (box model)
- Работать с **margin, padding, border**
- Использовать **box-sizing** правильно
- Различать **блочные и строчные** элементы
- Применять различные типы **позиционирования**
- Использовать **z-index** для управления слоями

---

## � Теоретическая часть

### Что такое блочная модель?

**Блочная модель (Box Model)** — это концепция, описывающая, как браузер рассчитывает размеры и расположение элементов на странице.

Каждый элемент состоит из 4 частей:

```
┌─────────────────────────────────────────────────────┐
│                    MARGIN                           │
│   ┌─────────────────────────────────────────────┐   │
│   │                 BORDER                      │   │
│   │   ┌─────────────────────────────────────┐   │   │
│   │   │              PADDING                │   │   │
│   │   │   ┌─────────────────────────────┐   │   │   │
│   │   │   │           CONTENT           │   │   │   │
│   │   │   │                             │   │   │   │
│   │   │   │   width × height            │   │   │   │
│   │   │   │                             │   │   │   │
│   │   │   └─────────────────────────────┘   │   │   │
│   │   │                                     │   │   │
│   │   └─────────────────────────────────────┘   │   │
│   │                                             │   │
│   └─────────────────────────────────────────────┘   │
│                                                     │
└─────────────────────────────────────────────────────┘
```

| Часть | Описание |
|-------|----------|
| **Content** | Содержимое элемента (текст, изображения) |
| **Padding** | Внутренний отступ между содержимым и границей |
| **Border** | Рамка вокруг содержимого и padding |
| **Margin** | Внешний отступ от других элементов |

---

## 📏 Margin — Внешние отступы

### Синтаксис

```css
/* Все стороны одинаково */
.box {
    margin: 20px;
}

/* Вертикаль / Горизонталь */
.box {
    margin: 10px 20px; /* верх-низ / лево-право */
}

/* Верх / Горизонталь / Низ */
.box {
    margin: 10px 20px 30px;
}

/* Все стороны отдельно (по часовой стрелке) */
.box {
    margin: 10px 20px 30px 40px; /* top right bottom left */
}

/* Каждая сторона отдельным свойством */
.box {
    margin-top: 10px;
    margin-right: 20px;
    margin-bottom: 30px;
    margin-left: 40px;
}
```

### Центрирование блока

```css
.centered-block {
    width: 800px;
    margin: 0 auto; /* 0 сверху/снизу, auto слева/справа */
}
```

### Схлопывание margin (margin collapsing)

```css
/* Вертикальные margin соседних элементов не суммируются,
   а схлопываются (берётся бОльший) */

.box1 {
    margin-bottom: 30px;
}

.box2 {
    margin-top: 20px;
}

/* Расстояние между ними будет 30px, а не 50px! */
```

### Отрицательные margin

```css
.overlap {
    margin-top: -20px; /* Элемент "налезет" на предыдущий */
}
```

---

## � Padding — Внутренние отступы

### Синтаксис

```css
/* Аналогичен margin */
.box {
    padding: 20px;              /* Все стороны */
    padding: 10px 20px;         /* Верт. / Гориз. */
    padding: 10px 20px 30px 40px; /* top right bottom left */
}

/* Отдельные свойства */
.box {
    padding-top: 10px;
    padding-right: 20px;
    padding-bottom: 30px;
    padding-left: 40px;
}
```

### Важные отличия от margin

| Padding | Margin |
|---------|--------|
| Внутри элемента | Снаружи элемента |
| Влияет на размер (без border-box) | Не влияет на размер |
| Не может быть отрицательным | Может быть отрицательным |
| Не схлопывается | Схлопывается по вертикали |

---

## 🖼 Border — Рамка

### Синтаксис

```css
/* Сокращённая запись */
.box {
    border: 2px solid #333;
    /* толщина | стиль | цвет */
}

/* Отдельные свойства */
.box {
    border-width: 2px;
    border-style: solid;
    border-color: #333;
}

/* Каждая сторона отдельно */
.box {
    border-top: 3px dashed red;
    border-right: 2px solid blue;
    border-bottom: 1px dotted green;
    border-left: none;
}
```

### Стили границ (border-style)

```css
.examples {
    border-style: solid;   /* Сплошная */
    border-style: dashed;  /* Пунктирная */
    border-style: dotted;  /* Точечная */
    border-style: double;  /* Двойная */
    border-style: groove;  /* Вдавленная */
    border-style: ridge;   /* Выпуклая */
    border-style: inset;   /* Внутренняя тень */
    border-style: outset;  /* Внешняя тень */
    border-style: none;    /* Без рамки */
}
```

### Скругление углов (border-radius)

```css
.box {
    border-radius: 10px;              /* Все углы */
    border-radius: 10px 20px;         /* верх-лево/низ-право верх-право/низ-лево */
    border-radius: 10px 20px 30px 40px; /* Каждый угол */
    border-radius: 50%;               /* Круг/Эллипс */
}

/* Отдельные углы */
.box {
    border-top-left-radius: 10px;
    border-top-right-radius: 20px;
    border-bottom-right-radius: 30px;
    border-bottom-left-radius: 40px;
}
```

---

## � Box-sizing

### Проблема content-box

По умолчанию `width` и `height` задают размер только **контента**:

```css
.box {
    box-sizing: content-box; /* По умолчанию */
    width: 200px;
    padding: 20px;
    border: 5px solid;
}
/* Итоговая ширина: 200 + 20*2 + 5*2 = 250px! */
```

### Решение: border-box

```css
.box {
    box-sizing: border-box;
    width: 200px;
    padding: 20px;
    border: 5px solid;
}
/* Итоговая ширина: 200px! (padding и border внутри) */
```

### Глобальный сброс (рекомендуется!)

```css
*, *::before, *::after {
    box-sizing: border-box;
}
```

---

## 🧱 Display — Тип отображения

### Основные значения

```css
.element {
    display: block;        /* Блочный */
    display: inline;       /* Строчный */
    display: inline-block; /* Строчно-блочный */
    display: none;         /* Скрытый */
    display: flex;         /* Flex-контейнер */
    display: grid;         /* Grid-контейнер */
}
```

### Сравнение block vs inline vs inline-block

| Свойство | block | inline | inline-block |
|----------|-------|--------|--------------|
| Новая строка | Да | Нет | Нет |
| Width/Height | Работает | Нет | Работает |
| Margin/Padding | Полностью | Только гориз. | Полностью |

---

## 📍 Position — Позиционирование

### Значения position

```css
.element {
    position: static;    /* По умолчанию, обычный поток */
    position: relative;  /* Относительно себя */
    position: absolute;  /* Относительно родителя с position */
    position: fixed;     /* Относительно окна браузера */
    position: sticky;    /* Гибрид relative и fixed */
}
```

### position: relative

```css
.box {
    position: relative;
    top: 20px;    /* Сдвинуть вниз на 20px */
    left: 30px;   /* Сдвинуть вправо на 30px */
}
/* Место в потоке сохраняется! */
```

### position: absolute

```css
.parent {
    position: relative; /* Обязательно! */
}

.child {
    position: absolute;
    top: 0;
    right: 0;
    /* Позиционируется относительно .parent */
}
```

### position: fixed

```css
.header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    /* Всегда вверху, при скролле не двигается */
}
```

### position: sticky

```css
.nav {
    position: sticky;
    top: 0;
    /* Ведёт себя как relative, пока не достигнет top: 0,
       затем "прилипает" как fixed */
}
```

---

## 📚 Z-index — Порядок слоёв

```css
.layer1 {
    position: relative;
    z-index: 1;
}

.layer2 {
    position: relative;
    z-index: 10; /* Выше layer1 */
}

.layer3 {
    position: relative;
    z-index: 100; /* Выше всех */
}
```

> **Важно:** z-index работает только с позиционированными элементами (не static)!

---

## 📁 Структура урока

```
05-css-box-model-and-layout/
│
├── README.md                    # Этот файл
├── examples/
│   ├── 01-box-model/            # Демонстрация блочной модели
│   ├── 02-margin-padding/       # Margin и padding
│   ├── 03-border/               # Границы и скругления
│   ├── 04-box-sizing/           # Content-box vs border-box
│   └── 05-positioning/          # Позиционирование
├── practice/
│   ├── 01-card-layout/          # Вёрстка карточки
│   └── 02-sticky-header/        # Липкая шапка
└── assets/
```

---

## 📖 Глоссарий

| Термин | Определение |
|--------|-------------|
| **Box Model** | Модель, описывающая структуру элемента |
| **Margin** | Внешний отступ элемента |
| **Padding** | Внутренний отступ элемента |
| **Border** | Рамка элемента |
| **Content** | Содержимое элемента |
| **Position** | Способ позиционирования элемента |
| **Z-index** | Порядок элемента по оси Z (глубина) |

