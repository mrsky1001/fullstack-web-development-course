# Практическое задание: Калькулятор

## 🎯 Цель

Создать простой калькулятор, используя функции JavaScript.

---

## 📋 Задание

### Функционал:

1. **Базовые операции:** сложение, вычитание, умножение, деление
2. **Ввод чисел:** через input поля
3. **Результат:** отображение на странице
4. **Валидация:** проверка на деление на ноль

### Требования к коду:

- Каждая операция — отдельная функция
- Используйте стрелочные функции
- Обработка ошибок (try/catch или условия)

---

## ✅ Критерии

- [ ] 4 функции для операций
- [ ] Корректное преобразование типов
- [ ] Обработка деления на ноль
- [ ] Чистый, читаемый код

---

## 💡 Подсказка

```javascript
const add = (a, b) => a + b;
const subtract = (a, b) => a - b;
const multiply = (a, b) => a * b;
const divide = (a, b) => {
    if (b === 0) return 'Ошибка: деление на ноль';
    return a / b;
};

function calculate(operation) {
    const a = parseFloat(document.getElementById('num1').value);
    const b = parseFloat(document.getElementById('num2').value);
    
    let result;
    switch(operation) {
        case 'add': result = add(a, b); break;
        // ...
    }
    
    document.getElementById('result').textContent = result;
}
```
