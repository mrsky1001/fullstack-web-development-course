# Практическое задание: Валидация формы

## 🎯 Цель

Реализовать валидацию формы в реальном времени.

---

## 📋 Задание

### Поля для валидации:

1. **Имя** — минимум 2 символа, только буквы
2. **Email** — корректный формат
3. **Телефон** — формат +7 (XXX) XXX-XX-XX
4. **Пароль** — минимум 8 символов, буквы и цифры
5. **Подтверждение пароля** — совпадение с паролем

### Требования:

- Валидация при вводе (событие `input`)
- Визуальная индикация (зелёный/красный border)
- Отображение сообщений об ошибках
- Кнопка Submit активна только при валидной форме

---

## ✅ Критерии

- [ ] Все поля валидируются
- [ ] Валидация в реальном времени
- [ ] Отображение ошибок под полями
- [ ] Визуальная обратная связь (цвета)
- [ ] Submit заблокирован при ошибках

---

## 💡 Подсказка

```javascript
function validateEmail(email) {
    const pattern = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    return pattern.test(email);
}

function validatePhone(phone) {
    const pattern = /^\+7 \(\d{3}\) \d{3}-\d{2}-\d{2}$/;
    return pattern.test(phone);
}

function showError(inputId, message) {
    const input = document.getElementById(inputId);
    const error = document.getElementById(inputId + 'Error');
    
    input.classList.add('invalid');
    input.classList.remove('valid');
    error.textContent = message;
}

function showSuccess(inputId) {
    const input = document.getElementById(inputId);
    const error = document.getElementById(inputId + 'Error');
    
    input.classList.add('valid');
    input.classList.remove('invalid');
    error.textContent = '';
}
```
