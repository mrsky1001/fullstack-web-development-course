# Практическое задание: Список задач (Todo List)

## 🎯 Цель

Создать список задач с использованием массивов и DOM.

---

## 📋 Задание

### Функционал:

1. **Добавление задачи** через input + кнопку
2. **Отображение списка** задач
3. **Отметка выполненной** (checkbox или клик)
4. **Удаление задачи**
5. **Фильтрация:** все / активные / выполненные

### Структура данных:

```javascript
const todos = [
    { id: 1, text: 'Изучить JavaScript', completed: false },
    { id: 2, text: 'Создать проект', completed: true }
];
```

---

## ✅ Критерии

- [ ] Массив объектов для хранения задач
- [ ] Добавление новых задач
- [ ] Переключение статуса
- [ ] Удаление задач
- [ ] Фильтрация (бонус)
- [ ] Сохранение в localStorage (бонус)

---

## 💡 Подсказка

```javascript
function addTodo(text) {
    todos.push({
        id: Date.now(),
        text: text,
        completed: false
    });
    renderTodos();
}

function toggleTodo(id) {
    const todo = todos.find(t => t.id === id);
    if (todo) {
        todo.completed = !todo.completed;
    }
    renderTodos();
}

function renderTodos() {
    const html = todos.map(todo => `
        <li class="${todo.completed ? 'completed' : ''}">
            ${todo.text}
        </li>
    `).join('');
    
    document.getElementById('todoList').innerHTML = html;
}
```
