# Урок 9: Валидация и Тестирование (Финальный урок)

## 🎯 Цель урока

Добавить валидацию данных, стандартизировать ответы API и подготовить приложение к production.

## 📚 Изучаемые концепции

1. **Валидация данных** — проверка входящих данных
2. **Стандартизированные ответы** — единый формат JSON
3. **Классы моделей** — User, Product, ResObj
4. **Константы сообщений** — централизованные тексты
5. **Best practices** — лучшие практики разработки

## 📁 Финальная структура проекта

```
09-testing-and-validation/
├── src/
│   ├── app.js                       # Главное приложение
│   ├── server.js                    # Точка входа
│   ├── models/                       # 🆕 Классы данных
│   │   ├── resObj.class.js          # Стандартный ответ
│   │   ├── user.class.js            # Пользователь
│   │   └── product.class.js         # Товар
│   ├── lib/                          # 🆕 Утилиты
│   │   ├── messages.lib.js          # Константы сообщений
│   │   └── mockProducts.js          # Тестовые данные
│   ├── services/
│   │   ├── db.service.js
│   │   ├── user.service.js
│   │   ├── product.service.js
│   │   └── shopping-cart.service.js
│   ├── controllers/
│   │   ├── auth.controller.js       # 🔄 + валидация
│   │   ├── product.controller.js
│   │   └── shopping-cart.controller.js
│   ├── routes/
│   │   ├── auth.router.js
│   │   ├── product.router.js
│   │   └── shopping-cart.router.js
│   └── middlewares/
│       ├── logger.middleware.js
│       └── auth.middleware.js
├── .env.example
├── package.json
└── README.md
```

## 🛠️ Новое в этом уроке

| Компонент | Описание |
|-----------|----------|
| `models/` | Классы для структурирования данных |
| `lib/` | Утилиты и константы |
| `ResObj` | Единый формат ответа API |
| Валидация | Проверка email, пароля, ID |

## 🚀 Запуск

```bash
cd lessons/09-testing-and-validation
npm install
copy .env.example .env
npm start
```

## 🔑 Ключевые моменты

### Класс ResObj

```javascript
class ResObj {
    constructor(options) {
        this.status = options.status >= 200 && options.status < 300 
            ? 'success' : 'error';
        this.statusCode = options.status ?? 200;
        this.message = options.text;
        if (options.data) this.data = options.data;
    }
}

// Использование
res.json(new ResObj({
    text: 'Операция успешна',
    data: products
}));
```

### Валидация входных данных

```javascript
exports.registration = async (req, res) => {
    const { email, password } = req.body;
    
    // Проверка наличия
    if (!email || !password) {
        return res.status(400).json(new ResObj({
            status: 400,
            text: 'Укажите email и password'
        }));
    }
    
    // Валидация формата email
    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
    if (!emailRegex.test(email)) {
        return res.status(400).json(new ResObj({
            status: 400,
            text: 'Некорректный формат email'
        }));
    }
    
    // Валидация длины пароля
    if (password.length < 6) {
        return res.status(400).json(new ResObj({
            status: 400,
            text: 'Пароль минимум 6 символов'
        }));
    }
    
    // ... продолжение
};
```

### Централизованные сообщения

```javascript
// lib/messages.lib.js
module.exports = {
    USER_NOT_AUTH: 'Пользователь не авторизован',
    PRODUCT_NOT_FOUND: 'Товар не найден',
    SUCCESS_OPERATION: 'Операция выполнена успешно'
};

// Использование
const msgs = require('../lib/messages.lib');
res.json(new ResObj({ text: msgs.SUCCESS_OPERATION }));
```

### Класс модели с безопасным JSON

```javascript
class User {
    toJSON() {
        return {
            id: this.id,
            name: this.name,
            email: this.email,
            role: this.role
            // password НЕ включаем!
        };
    }
}
```

## 📊 Полная архитектура

```
┌─────────────────────────────────────────────────────┐
│                      CLIENT                          │
└───────────────────────────────────────────────────────┘
                          ↓ HTTP Request
┌─────────────────────────────────────────────────────┐
│                      CORS                            │
└───────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                    MIDDLEWARE                        │
│  (Logger, JSON Parser, Session, Passport)           │
└───────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                     ROUTER                           │
│  (auth, product, shopping-cart)                     │
└───────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                   CONTROLLER                         │
│  (Валидация → Вызов Service → ResObj)               │
└───────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                    SERVICE                           │
│  (Бизнес-логика, работа с БД)                       │
└───────────────────────────────────────────────────────┘
                          ↓
┌─────────────────────────────────────────────────────┐
│                    DATABASE                          │
│  (MySQL / Mock Data)                                 │
└───────────────────────────────────────────────────────┘
```

## 🎉 Поздравляем!

Вы завершили курс разработки бэкенда! Теперь у вас есть полноценное REST API с:

- ✅ Express.js сервером
- ✅ Маршрутизацией и контроллерами
- ✅ Middleware (логи, CORS, сессии)
- ✅ MySQL базой данных
- ✅ CRUD операциями
- ✅ Аутентификацией (Passport.js)
- ✅ Защитой маршрутов
- ✅ Валидацией данных
- ✅ Стандартизированными ответами

## 📚 Что изучать дальше?

1. **TypeScript** — типизация JavaScript
2. **JWT токены** — альтернатива сессиям
3. **Docker** — контейнеризация
4. **Unit тесты** — Jest, Mocha
5. **API документация** — Swagger/OpenAPI
6. **Rate limiting** — защита от DDoS
7. **Логирование** — Winston, Morgan
