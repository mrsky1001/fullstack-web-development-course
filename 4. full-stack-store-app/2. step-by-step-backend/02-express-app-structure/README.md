# Урок 2: Установка Express.js и структура приложения

## 🎯 Цель урока

Установить фреймворк Express.js и создать правильную структуру приложения с разделением на `app.js` и `server.js`.

## 📚 Изучаемые концепции

1. **Express.js** — популярный веб-фреймворк для Node.js
2. **npm** — менеджер пакетов для установки зависимостей
3. **Разделение кода** — `app.js` (конфигурация) и `server.js` (запуск)
4. **Маршрутизация в Express** — `app.get()`, `app.post()` и др.
5. **Методы ответа** — `res.send()`, `res.json()`, `res.status()`

## 📁 Структура проекта

```
02-express-app-structure/
├── src/
│   ├── app.js         # Конфигурация Express (middleware, маршруты)
│   └── server.js      # Точка входа (запуск сервера)
├── package.json       # Зависимости проекта
└── README.md          # Этот файл
```

## 🛠️ Что нового добавлено

| Компонент | Описание |
|-----------|----------|
| `express` | Фреймворк для веб-серверов |
| `app.js` | Отдельный файл для конфигурации |
| `res.json()` | Отправка JSON ответов |
| `express.json()` | Middleware для парсинга JSON |

## 🚀 Запуск сервера

```bash
# 1. Перейдите в папку урока
cd lessons/02-express-app-structure

# 2. Установите зависимости
npm install

# 3. Запустите сервер
npm start

# или с автоперезагрузкой (Node.js 18+)
npm run dev
```

## ✅ Ожидаемый результат

| Маршрут | Метод | Описание |
|---------|-------|----------|
| `/` | GET | Главная страница (HTML) |
| `/about` | GET | О проекте (HTML) |
| `/api/status` | GET | Статус сервера (JSON) |
| `/api/products` | GET | Список товаров (JSON) |

## 🔑 Ключевые моменты

### Установка Express

```bash
npm install express
```

### Создание Express приложения

```javascript
const express = require('express');
const app = express();
```

### Определение маршрутов

```javascript
// GET запрос
app.get('/api/products', (req, res) => {
    res.json({ data: [...] });
});

// POST запрос
app.post('/api/products', (req, res) => {
    const data = req.body; // Данные из тела запроса
    res.json({ created: data });
});
```

### Middleware

```javascript
// Встроенные middleware
app.use(express.json()); // Парсит JSON тело запроса
app.use(express.urlencoded({ extended: true })); // Парсит form-data
```

## 🆚 Сравнение: Node.js vs Express

| Функция | Чистый Node.js | Express.js |
|---------|---------------|------------|
| Создание сервера | `http.createServer()` | `express()` |
| Маршруты | `if (req.url === ...)` | `app.get(path, handler)` |
| Отправка JSON | `res.end(JSON.stringify(...))` | `res.json(...)` |
| Парсинг body | Вручную | `express.json()` |

## 📝 Задания для практики

1. Добавьте маршрут `POST /api/products` для создания товара
2. Добавьте маршрут `GET /api/products/:id` для получения одного товара
3. Попробуйте отправить POST запрос через Postman или curl

## ➡️ Что дальше?

В следующем уроке мы организуем маршруты по отдельным файлам (Routers) и создадим контроллеры для обработки бизнес-логики.
