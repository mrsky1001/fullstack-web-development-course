# Урок 04: Переключение темы (светлая/темная)

## 🎯 Цели урока

После завершения этого урока вы будете:
- Понимать работу data-атрибутов в HTML
- Уметь использовать localStorage для хранения данных
- Создавать темы через переопределение CSS-переменных
- Определять системные настройки пользователя

## 📝 Что добавлено в этом уроке

### Структура файлов

```
04-theme-switcher/
├── index.html
├── css/
│   └── style.css   # Добавлены стили светлой темы
├── js/
│   ├── theme.js    # НОВОЕ: Переключение темы
│   └── menu.js
└── README.md
```

### Data-атрибуты

```html
<html lang="ru" data-theme="dark">
```

Data-атрибуты — это пользовательские атрибуты для хранения данных в HTML.

### CSS-селектор по атрибуту

```css
/* Темная тема (по умолчанию) */
:root {
    --color-bg: #0b0c15;
    --color-text: #ffffff;
}

/* Светлая тема */
[data-theme="light"] {
    --color-bg: #ffffff;
    --color-text: #111827;
}
```

### localStorage

```javascript
// Сохранение
localStorage.setItem('theme', 'dark');

// Загрузка
const theme = localStorage.getItem('theme');

// Удаление
localStorage.removeItem('theme');
```

### Определение системной темы

```javascript
const prefersDark = window.matchMedia('(prefers-color-scheme: dark)');
if (prefersDark.matches) {
    // Пользователь предпочитает темную тему
}
```

## 💡 Ключевые концепции

### Каскад CSS-переменных

1. Переменные объявлены в `:root` (темная тема)
2. При наличии `[data-theme="light"]` переменные переопределяются
3. Все элементы автоматически получают новые значения

### Плавные переходы

```css
body {
    transition: background-color 0.3s, color 0.3s;
}
```

### Показ/скрытие иконок

```css
/* По умолчанию показываем луну */
.sun-icon { display: none; }

/* В светлой теме - солнце */
[data-theme="light"] .sun-icon { display: block; }
[data-theme="light"] .moon-icon { display: none; }
```

## 🔍 Проверьте работу

1. Откройте `index.html` в браузере
2. Нажмите на кнопку с луной — тема станет светлой
3. Нажмите на солнце — вернется темная тема
4. Закройте браузер и откройте снова — тема сохранится!

## 🛠️ localStorage в DevTools

1. Откройте DevTools (F12)
2. Перейдите во вкладку Application → Local Storage
3. Вы увидите сохраненное значение темы

## 📚 Дополнительные материалы

- [MDN: Data attributes](https://developer.mozilla.org/ru/docs/Learn/HTML/Howto/Use_data_attributes)
- [MDN: localStorage](https://developer.mozilla.org/ru/docs/Web/API/Window/localStorage)
- [MDN: matchMedia](https://developer.mozilla.org/ru/docs/Web/API/Window/matchMedia)
